<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.memories">

    <application
        android:allowBackup="true"
        android:icon="@drawable/notes"
        android:label="@string/app_name"
        android:roundIcon="@drawable/notes"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".ShowAllApps"></activity>
        <activity android:name=".AddNewApp" />
        <activity android:name=".AddNewToDo" />
        <activity android:name=".ContinueAccountActivity" />
        <activity android:name=".FirstActivtiy" />
        <activity android:name=".ForgetPasswordActivity" />
        <activity android:name=".SignActivity" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>